name: bot-test

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: bot-test
  cancel-in-progress: false

jobs:
  bot:
    uses: ./.github/workflows/release-bot.yml